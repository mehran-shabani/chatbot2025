# راهنمای جامع پروژه برای ایجنت‌های توسعه‌دهنده

این سند به عنوان یک راهنمای کامل برای توسعه پروژه چت‌بات هوش مصنوعی برای پژوهشگران فارسی‌زبان عمل می‌کند. تمام ایجنت‌ها باید قبل از شروع به کار، این سند را به دقت مطالعه کرده و به آن پایبند باشند.

## ۱. نمای کلی پروژه

- **محصول:** یک چت‌بات هوش مصنوعی تجاری (SaaS) که برای کمک به پژوهشگران فارسی‌زبان طراحی شده است.
- **زبان:** رابط کاربری (UI) و تجربه کاربری (UX) باید کاملاً به زبان **فارسی** باشد.

## ۲. پشته فناوری (Technology Stack)

- **فرانت‌اند:** Next.js (همراه با TypeScript, Tailwind CSS, ESLint)
- **بک‌اند:** FastAPI (بر پایه Python)
- **پایگاه داده اصلی:** PostgreSQL
- **مدیریت مهاجرت‌های پایگاه داده (Migrations):** Alembic
- **پایگاه داده برداری (Vector Store):** ChromaDB
- **صف وظایف (Task Queue):** Celery (همراه با Redis به عنوان Broker)
- **هوش مصنوعی:** کتابخانه OpenAI
- **احراز هویت:** سیستم بدون رمز عبور از طریق ارسال کد یکبار مصرف (OTP) به شماره موبایل کاربر با استفاده از سرویس **کاوه نگار (Kavenegar)**.
- **درگاه پرداخت:** **بیت‌پی ایران (bitpay.ir)**

## ۳. راه‌اندازی محیط توسعه

### بک‌اند (Backend)
1.  **ایجاد محیط مجازی پایتون:**
    ```bash
    python3 -m venv backend/venv
    ```
2.  **فعال‌سازی محیط مجازی:**
    - در macOS/Linux:
      ```bash
      source backend/venv/bin/activate
      ```
    - در Windows:
      ```bash
      backend\\venv\\Scripts\\activate
      ```
3.  **نصب وابستگی‌ها:**
    ```bash
    pip install -r backend/requirements.txt
    ```

### فرانت‌اند (Frontend)
1.  **ایجاد پروژه Next.js (فقط برای راه‌اندازی اولیه):**
    ```bash
    npx create-next-app@latest frontend --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --no-turbopack
    ```
2.  **نصب وابستگی‌ها:**
    ```bash
    cd frontend
    npm install
    ```

### فایل `.gitignore`
اطمینان حاصل کنید که مسیرهای زیر به فایل `.gitignore` اضافه شده‌اند تا از ورود فایل‌های غیرضروری به ریپازیتوری جلوگیری شود:
```
backend/venv/
frontend/node_modules/
```

## ۴. مدیریت پیکربندی (Configuration)

- **اکیداً ممنوع:** هیچ‌گونه اطلاعات حساس مانند کلیدهای API، اطلاعات اتصال به پایگاه داده و... نباید در کد به صورت **هاردکد** نوشته شود.
- **راهکار:** تمام تنظیمات و متغیرهای حساس باید از طریق **متغیرهای محیطی (Environment Variables)** مدیریت شوند. در بک‌اند، برای این کار از کتابخانه `pydantic-settings` استفاده خواهد شد.

## ۵. فرآیند برنامه‌ریزی و توسعه

### اصل اول: برنامه‌ریزی عمیق
قبل از نوشتن هر قطعه کد، ایجنت باید وارد **"حالت برنامه‌ریزی عمیق"** شود. این به معنای پرسیدن سوالات شفاف‌کننده برای درک کامل نیازمندی‌ها و ارائه یک طرح اجرایی دقیق است.

### نقشه راه توسعه (Roadmap)

1.  **فاز اول: توسعه بک‌اند**
    -   طراحی و پیاده‌سازی ساختار اولیه پروژه FastAPI.
    -   تعریف مدل‌های داده (Data Models) با استفاده از SQLAlchemy و ایجاد اولین مهاجرت (Migration) با Alembic.
    -   پیاده‌سازی سیستم احراز هویت بدون رمز عبور با سرویس کاوه نگار.
    -   راه‌اندازی Celery و Redis برای مدیریت وظایف پس‌زمینه (مانند ارسال پیامک).
    -   یکپارچه‌سازی با ChromaDB برای ذخیره و بازیابی اطلاعات برداری.
    -   اتصال به API کتابخانه OpenAI برای پردازش درخواست‌های کاربر و ایجاد منطق اصلی چت‌بات.
    -   پیاده‌سازی API‌های مربوط به درگاه پرداخت بیت‌پی ایران.
    -   نوشتن تست‌های واحد (Unit Tests) و یکپارچه‌سازی (Integration Tests) برای اطمینان از صحت عملکرد.

2.  **فاز دوم: توسعه فرانت‌اند**
    -   طراحی و پیاده‌سازی رابط کاربری (UI) کاملاً فارسی و واکنش‌گرا (Responsive).
    -   اتصال به API‌های بک‌اند برای دریافت و ارسال داده.
    -   پیاده‌سازی کامل جریان ورود و احراز هویت کاربر.
    -   ساخت رابط کاربری صفحه چت.
    -   پیاده‌سازی صفحات و منطق مربوط به پروفایل کاربری و پرداخت‌ها.
    -   نوشتن تست‌های فرانت‌اند برای کامپوننت‌ها و جریان‌های کاربری اصلی.

3.  **فاز سوم: استقرار (Deployment)**
    -   کانتینرسازی اپلیکیشن (بک‌اند و فرانت‌اند) با استفاده از Docker.
    -   ایجاد یک خط لوله یکپارچه‌سازی و تحویل مداوم (CI/CD) برای خودکارسازی فرآیندهای تست و استقرار.
    -   استقرار نهایی پروژه روی یک ارائه‌دهنده خدمات ابری.
